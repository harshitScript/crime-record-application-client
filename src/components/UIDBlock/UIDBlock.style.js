import styled from "styled-components/macro";
import { SubTitleSm } from "../Typography/SubTitle";

export const UIDOuter = styled(SubTitleSm)`
  padding: 0.2rem;
  border: 1px dashed #000;
  display: flex;
  align-items: center;
  justify-content: center;
  width: max-content;
  margin: 0 auto;
  font-size: 0.5rem;
`;
